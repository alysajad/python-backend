#tuples are immutable
#tuples are faster than lists
three_num=(1,2,3)
print(three_num)
#three_num[0]=4#cant be changed
print(three_num[0])
# tuplea allow repettition of values
print(type(three_num))
strings =('home', 'labd', 'kill me', 'true')
#tuples can have multiple kinds of data
